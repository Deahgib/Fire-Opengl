#pragma once

#define IX(i, j) ((i) + (N+2)*(j))

namespace octet {
  /// Scene containing a particle system
  class fluid_simulator {
    float * u;

  public:
    fluid_simulator() {}
  
    void init() {

    }
  };

}
